// import { axiosInstance } from "@/app/lib/axiosInstance";

/**
 * Fetches a short-lived presigned URL for a PDF document so the citation
 * panel can open the correct page in an iframe.
 *
 * Wire this up when the documents API is ready.
 */
// export async function fetchPdfCitationUrl(documentId: string): Promise<string> {
//   const res = await axiosInstance.get<{ url: string }>(
//     `/api/documents/${documentId}/presigned-url`,
//   );
//   return res.data.url;
// }
